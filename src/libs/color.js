export const toHslString = ([h, s, l]) => `hsl(${h}, ${s}%, ${l}%)`;
export const toHslaString = ([h, s, l, a]) => `hsla(${h}, ${s}%, ${l}%, ${a})`;
export const toRgbString = ([r, g, b]) => `rgb(${r}, ${g}, ${b})`;
export const toRgbaString = ([r, g, b, a]) => `rgba(${r}, ${g}, ${b}, ${a})`;
