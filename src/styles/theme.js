// colors are in HSLA array format:
// [hue, saturation, luminosity]

const theme = {
  palette: {
    greyscale: {
      black: [0, 0, 15],
      white: [0, 0, 98]
    }
  }
};

export default theme;
